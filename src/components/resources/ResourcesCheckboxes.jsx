import { Grid } from "@mui/material";

const ResourcesCheckboxes = () => {
  return (
    <Grid
      item
      sx={{
        display: "flex",
        flexDirection: "column",
        flexWrap: "wrap",
      }}>
      {/* Put checkboxes here */}
    </Grid>
  );
};

export default ResourcesCheckboxes;
